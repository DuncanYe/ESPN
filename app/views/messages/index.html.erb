<% @messages.each do |message| %>
  <div class="col-me-10 col-md-offset-1">
    <div class="message">
      <h2 class="message-title"><%= message.title %></h2>
      <%= link_to 'Join convo', message, class: 'btn-custom' %>
    </div>
  </div>

<% end %>

